import { Ng2SmoothScrollDirective } from './ng2-smooth-scroll.directive';

describe('Ng2SmoothScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new Ng2SmoothScrollDirective();
    expect(directive).toBeTruthy();
  });
});
